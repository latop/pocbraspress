export interface LocationGroup {
  code: string;
  description: string;
}

export interface Location {
  name: string;
  code: string;
  id: string;
}

export interface TripType {
  code: string;
  description: string;
}
